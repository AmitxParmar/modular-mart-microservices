name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint & Typecheck
        run: pnpm turbo run lint check-types

      - name: Build
        run: pnpm turbo run build

  deploy-frontend:
    needs: build-and-test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-version: vercel@latest
          working-directory: ./apps/web
          vercel-args: '--prod'

  #deploy-backend:
  #  needs: build-and-test
  #  if: github.ref == 'refs/heads/main'
  #  runs-on: ubuntu-latest
  #  steps:
  #    - name: Trigger Render Deploy
        # Render doesn't have an official action, so we use their deploy hook
        # or API. Using the Blueprint Sync hook is easiest.
  #      run: |
  #        curl -X POST "https://api.render.com/deploy/${{ secrets.RENDER_BLUEPRINT_ID }}?key=${{ secrets.RENDER_DEPLOY_KEY }}"
